---
uid: TestFramework.Core.Variables
---

**Consumer-first API.** `Var.Const(...)`, `Var.Ref<T>(...)` and `Var.RefImmutable<T>(...)` are how data
reaches a step, and almost every timeline uses them directly.

New to this? [Passing data between steps](../learn/passing-data.md) walks it in order.
